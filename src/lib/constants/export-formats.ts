/** Life Sciences / moderator export defaults (from parent interview). */
export const LIFE_SCIENCES_EXPORT_DEFAULTS = {
  fontFamily: "Arial",
  fontSizePt: 12,
  lineSpacing: 1.5,
  linedPaper: true,
  extraLinesForHandwriting: true,
  lineGap: 2,
  format: "pdf" as const,
};

/** Mathematics — GDE department template (upload in Phase 2). */
export const MATHS_EXPORT_NOTES = {
  templateSource: "GDE memo template (department standard)",
  requiresCognitiveReport: true,
};
